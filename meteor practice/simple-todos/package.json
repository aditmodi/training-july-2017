{
  "name": "simple-todos",
  "private": true,
  "scripts": {
    "start": "meteor --settings settings.json",
    "test":"meteor test --driver-package practicalmeteor:mocha --settings settings.json --port 8000"
  },
  "dependencies": {
    "babel-runtime": "^6.23.0",
    "bcrypt": "^1.0.2",
    "meteor-node-stubs": "~0.2.4"
  }
}
